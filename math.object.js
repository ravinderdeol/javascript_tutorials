// floor() rounds down to the nearest integer
floor = Math.floor(1.5);
console.log(floor); // returns 1

// trunc() returns the integer part of a number
trunc = Math.trunc(1.5);
console.log(trunc); // returns 1
